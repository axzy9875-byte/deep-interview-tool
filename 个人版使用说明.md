# 我的 AI 访谈 · 管理端与公开访谈版

双击同目录的 **启动我的AI访谈.command**，然后打开 http://localhost:5173/admin 。使用时保留启动窗口，按 Control+C 停止。本机已安装所需运行环境。本机未设置管理员密码时，仅回环地址自动进入管理端。

## 公开访谈版

管理端位于 `/admin`，包含API配置、内容准备、访谈、写稿和公开访谈管理。受访者只能通过 `/join/访谈编号` 形式的专属链接进入受访页面，无法看到管理功能和API配置。

在管理端打开“公开访谈管理”，当前访谈的问题会显示为待发布清单。填写访谈名称和说明后点击“生成受访链接”。受访者打开链接后需要确认回答会被保存，可以填写称呼或匿名开始。每提交一题都会保存到服务器；“暂停并保存”可保留当前进度，同一浏览器再次打开链接会恢复。完成后可以点击“再进行一次访谈”，每次记录使用相同受访分组但具有独立次数、开始时间、最后保存时间和完成时间。

管理端按访谈列出所有记录，可查看每一道原题与回答，并单独导出Markdown。服务器数据默认保存在 `server-data/public-interviews.json`；公开部署时必须通过 `INTERVIEW_DATA_FILE` 指向持久化磁盘，例如 `/data/public-interviews.json`。

“公开访谈管理”中可以单独启用公开访客语音转写。点击“填入本机已配置的方舟密钥”并保存后，密钥会保存到服务器专用配置文件，公开页面只获得“是否启用”状态，不会收到密钥。公开访客可以分段录音，服务器调用 Seed-2.0-lite 完成转写并将文字追加到回答框。

公开语音额度按服务器签发的匿名访客 Cookie 累计，每位访客最多 `200,000 tokens`，同一访客的暂停恢复和多次访谈共用额度。服务器使用方舟响应中的 `usage.total_tokens` 记账；旧兼容响应缺少 usage 时按录音时长和文本长度保守估算。达到限额后服务器不再发起转写，访客仍可用文字完成访谈。管理员可在访谈记录表查看用量。访客清除 Cookie 或更换浏览器会形成新的匿名身份；如果以后需要防止主动规避额度，应再增加短信、邮箱或受访邀请码认证。

公开部署必须设置 `ADMIN_PASSWORD`、`ADMIN_COOKIE_SECRET`、`PUBLIC_ORIGIN`、`HOST=0.0.0.0` 和平台提供的 `PORT`。项目已包含 `Dockerfile` 与 `.env.example`。管理员登录与访客额度身份都使用仅服务器可读的签名 HttpOnly Cookie；受访者页面不会收到API密钥。公开语音配置文件通过 `PUBLIC_SPEECH_CONFIG_FILE` 指向持久化磁盘，例如 `/data/public-speech.json`。更推荐在部署平台设置 `PUBLIC_SPEECH_API_KEY` 和 `PUBLIC_SPEECH_MODEL`，环境变量存在时网页不能覆盖密钥。

## 文本生成：DeepSeek 与豆包自由切换

1. 在“API 服务商”选择 DeepSeek 或火山方舟·豆包，填写该平台的 API 密钥。
2. DeepSeek 预设 deepseek-chat；豆包预设 Doubao-Seed-2.0-lite（260428/260215）。也可填写控制台的完整模型 ID 或 ep- 开头的推理接入点 ID。
3. 点击“验证 API”做一次真实调用，成功后配置会保存。验证也可能产生少量 API 费用。
4. 各服务商已保存的模型和密钥分别保留。切换服务商后点击“保存全部配置”；访谈和写稿均使用当前选择。可在访谈或写稿前返回“API 配置”切换，无需重建访谈。

## 访谈内容准备：两种提问方式

在内容分析下方的“问题预览设置”中选择问题来源，切换方式保留现有分析：

- **严格按访谈提纲提问**：上传或粘贴提纲并添加为材料，切换严格模式时会直接显示第一份材料中的原题；也可选择其他提纲，点击“按所选提纲原文刷新预览”。对于带编号的提纲，编号题目是识别依据，“个人信息：”一类短标题及其说明不会再被当成独立问题。预览中可随时新增、删除、修改、上移或下移问题，即使浏览器里已有旧访谈也可以编辑；这些编辑不会覆盖旧问答。勾选核对确认后点击“按提纲开始访谈”。访谈会按确认清单的原文和顺序提问，不增加追问。最后一题回答或跳过后进入生成访谈稿；提纲少于 5 题也可使用。
- **AI 分析材料，生成访谈问题**：添加材料后点击“开始分析内容”，再生成问题预览。AI 会接收原始材料、摘要与问答历史，并可根据回答调整提问。模型返回 JSON 对象、JSON 数组、Markdown 代码块或编号问题清单时都能正常识别；如果 API 请求本身失败，页面会显示服务商返回的具体错误，不使用默认创作者问题替代。

现有文件上传支持 TXT、Markdown、JSON、CSV 等纯文本；Word/PDF 请先复制其提纲文字到文本框。复杂提纲可能包含说明、标题和多级追问，务必核对导入后的清单。刷新预览会用所选提纲替换问题清单，请先保存必要的手动修改。

已有访谈时仍可切换问题预览、编辑提纲原题和查看完整分析。在提纲模式勾选核对后，可直接点击“备份旧访谈并按提纲开始”；系统会先保存旧问答与稿件，再用当前清单进入新访谈。也可以先点击“备份旧记录，保留材料和分析重新设置访谈”单独重置。历史备份可通过“下载已备份的访谈记录”导出，备份失败不会重置当前访谈。刷新会恢复已保存的材料、分析和进度。

## Seed-2.0-lite 260428 语音转文字

1. 刷新网页，开启“启用语音识别 API”，选择“火山方舟 · Seed-2.0-lite 260428 转写”。
2. 填写火山方舟 API Key；也可点击“填入已配置的方舟密钥”，从文本配置中填入。无需另外开通录音文件极速版。
3. 模型填写 `doubao-seed-2-0-lite-260428`，或该模型对应的 `ep-` 推理接入点 ID。260215 不作为此音频转写方案的预设。
4. 点击“验证语音 API”，录一句话后点击“停止录音并验证”。也可选短音频后验证。成功后自动保存配置，失败会显示服务商错误码和说明。
5. 访谈时点击麦克风开始录音，再点击停止，转写结果进入回答框；核对后提交。

语音配置独立保存。访谈和写稿可以使用 DeepSeek，录音仍由 Seed 转写。每段录音最多 3 分钟，文件小于 20 MB；长回答可分段录入。它是停止录音后的转写，不是实时字幕。录音会发送到方舟并按模型调用规则计费。通用模型转写仍可能误听或改写，重要内容应核对录音。

原有“豆包语音·录音文件极速版”作为可选兼容方案保留，需独立的语音服务凭据，与上面的方舟方案不同。旧的极速版配置进入页面时默认转为 Seed 方案，密钥只从已保存的方舟配置填入，避免误用旧凭据；验证或保存后生效。

## 生成访谈稿

完成访谈后可在快速模式选择写作风格并点击“生成访谈稿”。“问答风格”直接从原始记录按顺序生成完整问答稿，不调用 AI，不删减任何一组问答；完成后会显示“已完整包含 N/N 组问答”。其他文章风格会调用当前所选模型：豆包 Seed 使用完整响应生成长稿；其他模型保留流式显示，如果流式接口未返回正文，系统会自动改用完整响应重试。模型返回空内容时不会保存为“0字访谈稿”，页面会显示具体错误以便检查模型或接入点。旧版本留下的空稿或不完整问答稿会在刷新时自动清理，原始问答不会受到影响。

## 数据和使用范围

密钥与访谈记录保存在当前浏览器。请固定使用同一个浏览器和 `http://localhost:5173`；清除浏览器数据会丢失记录，重要访谈应导出。密钥在浏览器中的固定密钥加密仅用于简单遮蔽，不能替代系统密码保险库。

本机服务将请求转发给选中的模型服务商，解决跨域限制；不记录密钥、录音或请求内容。文本材料和问答会发送给所选文本服务，录音会发送给所选转写服务。没有内置共享 API 密钥。

下载目录中的原始项目保持原样，此目录为独立个人版。原作者 MIT 许可证保留在 LICENSE。

## 已验证 / 待验证

已通过生产构建及 24 项配置、管理员鉴权、公开受访流程、暂停恢复、多次访谈、服务器存储、公开语音转写、匿名访客额度、提纲、问题预览、完整问答稿生成和接口代理自动化测试。已在浏览器实际验证公开链接开始访谈、暂停保存、继续并恢复原回答，以及管理端查看和导出记录。自动化接口测试使用模拟响应，不消耗账号额度。

尚未提供真实 API 凭据，未使用真实密钥验证实际模型回答与麦克风识别效果。请在页面填写凭据后分别验证文本 API 和语音识别。

## 后续上线

公开版必须部署为Node Web服务，不能只上传 `dist` 静态文件。访谈记录写入服务器，因此托管平台必须挂载持久化磁盘；如果没有磁盘，平台重启或重新部署会丢失记录。部署后使用平台提供的HTTPS子域名即可，也可以再绑定自有域名。

项目已包含 Render Blueprint `render.yaml`：使用 Singapore 区域、Docker、最低付费 Web Service 和 1GB 持久化磁盘，健康检查地址为 `/api/health`。部署时只需在 Render 的 Secret 表单中填写 `ADMIN_PASSWORD` 和 `PUBLIC_SPEECH_API_KEY`；`ADMIN_COOKIE_SECRET` 由 Render 自动生成。密钥不会进入 Git 仓库。

按当前 Render 官方价格，最低付费 Web Service 约 7 美元/月，1GB 磁盘约 0.25 美元/月，带宽超出免费量后另计。免费 Web Service 不能挂载持久化磁盘，不适合保存正式访谈记录。

开发命令：`npm ci`、`npm run dev`。更新发布文件：`npm run build`。运行发布版：`npm start`。测试：`npm test`。

## 接口资料

- 豆包语音极速版：https://www.volcengine.com/docs/6561/1631584
- 豆包 Seed 2.0 Lite：https://www.volcengine.com/docs/82379/1795150
- 原项目：https://github.com/easychen/ainterview

Seed 音频请求格式依据官方 SDK：https://github.com/volcengine/volcengine-python-sdk/blob/master/volcenginesdkarkruntime/types/chat/chat_completion_content_part_audio_param.py
